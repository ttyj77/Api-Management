<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ipa.openapi_inzent.dao.ApiDetailsDao">
    <select id="selectAll" parameterType="int" resultType="com.ipa.openapi_inzent.model.ApiDetailsDTO">
        select * from public."apiDetails"
        inner join public.api
        on public.api.id = public."apiDetails".id
        where public.api."apisId" = #{id};
    </select>
    <select id="selectOne" parameterType="int" resultType="com.ipa.openapi_inzent.model.ApiDetailsDTO">
        SELECT * FROM public."apiDetails"
        WHERE id = #{id};
    </select>

</mapper>
